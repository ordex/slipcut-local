# SlipCut Local

Split a multi-payslip PDF into one file per employee and prepare the matching
bank payment files — entirely in the browser. No upload, no cookies, no
tracking.

Plain ES modules: no build step, no Node, no npm.
